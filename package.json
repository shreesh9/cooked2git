{
  "name": "cooked2git",
  "version": "0.1.0",
  "description": "Auto-sync your LeetCode & NeetCode submissions to GitHub. Zero servers, zero telemetry.",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "postinstall": "node scripts/setup.mjs"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.25",
    "@types/chrome": "^0.0.287",
    "sharp": "^0.35.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.14",
    "vitest": "^2.1.8"
  }
}
