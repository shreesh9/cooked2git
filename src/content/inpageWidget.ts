/**
 * inpageWidget.ts — No-op file (Host page UI widget disabled per user request).
 * All sync info, streak, recent commits, and status are accessible inside the extension popup.
 */

export class InpageWidget {
  public static render(): void {
    // Disabled — no host page overlays
  }
}
